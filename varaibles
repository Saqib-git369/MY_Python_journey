In python Variables are the containers to store the data.
analogy:- like in our kitchen a jar containig salt , so here the salt is data and jar is variables.
eg:- x=5, car_name = "Toyota" (so car_name, x are variables and 5 , Toyota are the data .
